import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import Navbar from "@/components/shared/Navbar";
import Footer from "@/components/shared/Footer";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Vapi Punjabi Charitable Foundation",
  description: "A non-profit organization dedicated to serving the community through healthcare, education, and financial relief initiatives.",
  openGraph: {
    title: "Vapi Punjabi Charitable Foundation",
    description: "A non-profit organization dedicated to serving the community.",
    url: "https://vapipunjabicharitablefoundation.org",
    siteName: "Vapi Punjabi Charitable Foundation",
    images: [
      {
        url: "/og-image.jpg",
        width: 1200,
        height: 630,
      },
    ],
    locale: "en_US",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body className={`${inter.className} min-h-screen flex flex-col`}>
        <Navbar />
        <main className="flex-1">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
